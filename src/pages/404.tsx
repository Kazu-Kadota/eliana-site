import React, { Fragment } from 'react'

import NotFoundError from '@/components/error/404'

const NotFoundErrorRender = () => {
  return (
    <Fragment>
      <NotFoundError />
    </Fragment>
  )
}

export default NotFoundErrorRender
